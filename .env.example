# Copy to .env and adjust. All bulk data lives here, never in the repo:
# the internal disk on this machine has <2 GB free.
DATA_ROOT=/Volumes/T7/tacticalgraph

# Optional. Without a key, wandb runs in offline mode and nothing blocks on an account.
# WANDB_API_KEY=
WANDB_MODE=offline
